---
id: PLF-022
title: "Add planned ticket implementation plan command"
type: feature
created: 2026-06-24 14:30:12
dependencies: ["PLF-019", "PLF-020", "PLF-029"]
---

# PLF-022: Add planned ticket implementation plan command

## Objective

Add a read-only advisory command that produces an implementation plan for a ticket in `tickets/planned/`.

## Context

Planned tickets should be implementable by a fresh Pi session. A dedicated implementation-plan command can prepare the work in a separate context by reading the ticket, relevant docs, and likely code paths, then producing a durable plan for the orchestrator and human to review before activation.

## Scope

In scope:

- Add a command such as `/ticket-plan <ticket-id>`.
- Require the target ticket to exist in `tickets/planned/`.
- Check dependencies enough to warn about unresolved items.
- Use read-only child Pi analysis.
- Write an advisory implementation plan to the agreed artifact location.
- Include objective restatement, scope boundaries, likely files, step-by-step implementation approach, verification plan, risks, and stop conditions.
- Map acceptance criteria to verification steps where practical.
- Hand the advisory plan back to the parent LLM according to the `PLF-029` convention.

Out of scope:

- Activating the ticket.
- Editing code.
- Editing ticket scope.
- Resolving dependencies automatically.
- Running a write-capable implementation child.

## Acceptance Criteria

- [x] The command refuses tickets that are not currently in `tickets/planned/`.
- [x] The generated plan includes an advisory notice and source ticket fingerprint.
- [x] The plan identifies likely files and explicitly marks uncertainty where present.
- [x] The plan maps ticket acceptance criteria to implementation and verification steps.
- [x] The plan includes stop conditions for scope drift or missing information.
- [x] The command hands the plan back to the parent LLM or prints the suggested handoff in print mode.
- [x] The command does not activate or modify the ticket.

## Dependencies

- `PLF-019` — shared advisory artifact conventions.
- `PLF-020` — read-only child Pi runner.
- `PLF-029` — parent LLM handoff convention for advisory command results.

## Implementation Notes

Likely files:

- `.pi/extensions/ticket-workflow/`
- `.pi/extensions/ticket-workflow/prompts/plan.md` or equivalent inline prompt helper
- `tickets/.artifacts/plans/`
- `docs/ticket-workflow-commands.md`

This command should make the later `planned/` -> `ongoing/` decision easier, but should not perform that transition.

## Verification

- `npm --prefix .pi/extensions exec tsc -- -p .pi/extensions/tsconfig.json --noEmit` passed after the extension change.
- `pi --approve --offline --no-session -p '/ticket-status'` passed and reported `PLF-022` as the active ticket during implementation.
- `pi --approve --offline --no-session -p '/ticket-plan PLF-022'` refused clearly because `PLF-022` is in `tickets/ongoing/`, not `tickets/planned/`.
- Direct real-child verification with a temporary planned fixture started `/ticket-plan` successfully but the child Pi provider returned `Codex error: The usage limit has been reached` before artifact generation.
- A compiled-command fixture with a controlled read-only child response verified `/ticket-plan PLF-999` writes `tickets/.artifacts/plans/PLF-999.md`, includes advisory metadata and source SHA-256, includes likely files, maps acceptance criteria to verification, includes stop conditions, prints the suggested parent handoff in print mode, and does not move the planned ticket.
- Temporary verification files `tickets/planned/PLF-999.md`, `tickets/.artifacts/plans/PLF-999.md`, and the fresh readiness artifact `tickets/.artifacts/readiness/PLF-022.md` were removed after inspection.
- Final `pi --approve --offline --no-session -p '/ticket-status'` passed after moving the ticket to completed and reported no active ticket.
- Final `npm --prefix .pi/extensions exec tsc -- -p .pi/extensions/tsconfig.json --noEmit` and `git diff --check` passed.

## Files Changed

- `.pi/extensions/ticket-workflow/index.ts`
- `docs/ticket-workflow-commands.md`
- `tickets/completed/PLF-022.md`

## Decisions

- Implemented `/ticket-plan <ticket-id>` as a read-only advisory command that only accepts tickets in `tickets/planned/`.
- Added a small parent dependency pre-check based on frontmatter dependency IDs so unresolved dependencies are visible before the child plan.
- Kept the child analysis on the existing read-only runner safety boundary; the parent command remains responsible for writing the advisory artifact.
- Reused existing advisory artifact metadata and parent handoff helpers so implementation plans follow the same conventions as readiness briefs.
- Used an inline prompt helper rather than adding a separate prompt file to keep the MVP implementation small.

## Notes

This command becomes most useful after the readiness command starts producing better planned tickets, but it can be implemented independently once the child runner exists.

## Resolution

Implemented `/ticket-plan <ticket-id>` with planned-only validation, dependency pre-checks, read-only child Pi analysis, implementation-plan artifact generation, and parent LLM handoff behavior. The final commit message includes `PLF-022`; the commit identifier is discoverable with `git log --grep='PLF-022'`.

## Log

- 2026-06-24 14:30:12: Ticket created in backlog from the sub-Pi ticketization proposal for the ticket workflow command system.
- 2026-06-24 16:01:48: Updated to depend on `PLF-029` so implementation plans are handed back to the parent LLM instead of only being displayed or written as artifacts.
- 2026-06-24 16:35:15: Fresh `/ticket-readiness PLF-022` advisory verdict was `ready`; moved to planned after human approval to implement this ticket next.
- 2026-06-24 16:35:27: Activated for implementation after confirming no other ongoing ticket exists and dependencies are completed.
- 2026-06-24 16:39:52: Implemented `/ticket-plan`, updated documentation, and verified TypeScript, ticket status, planned-only refusal, artifact metadata/handoff behavior with a controlled child fixture, and cleanup of temporary artifacts.
- 2026-06-24 16:40:31: Moved to completed after implementation and verification; final commit pending.
- 2026-06-24 16:40:53: Final `/ticket-status`, TypeScript, and `git diff --check` verification passed with no active ticket.
