---
id: PLF-021
title: "Add backlog ticket readiness brief command"
type: feature
created: 2026-06-24 14:30:12
dependencies: ["PLF-019", "PLF-020", "PLF-029"]
---

# PLF-021: Add backlog ticket readiness brief command

## Objective

Add a read-only advisory command that analyzes whether a backlog ticket is ready to move to `planned/` and writes a readiness brief.

## Context

Backlog-to-planned refinement is one of the most repeated and context-heavy parts of the current workflow. A child Pi session can do the reading and analysis in a separate context, while the main orchestrator and human retain control over any actual ticket transition.

## Scope

In scope:

- Add a command such as `/ticket-readiness <ticket-id>`.
- Require the target ticket to exist in `tickets/backlog/`.
- Read the ticket workflow documentation and the target ticket.
- Use deterministic checks plus read-only child Pi analysis where useful.
- Write an advisory readiness brief to the agreed artifact location.
- Include a verdict such as `ready`, `not-ready`, `split-recommended`, or `defer-or-reject`.
- Identify missing objective, scope, acceptance criteria, dependencies, likely artifacts, and verification details.
- Include concrete human questions when the ticket is not ready.
- Hand the advisory result back to the parent LLM according to the `PLF-029` convention.

Out of scope:

- Moving the ticket to `planned/`.
- Editing the ticket.
- Creating split tickets automatically.
- Resolving dependencies automatically.

## Acceptance Criteria

- [x] The command refuses tickets that are not currently in `tickets/backlog/`.
- [x] The generated brief includes an advisory notice and source ticket fingerprint.
- [x] The brief evaluates the workflow definition of ready.
- [x] The brief lists missing information or blockers when the ticket is not ready.
- [x] The brief recommends a next action without performing it.
- [x] The command hands the readiness result back to the parent LLM or prints the suggested handoff in print mode.
- [x] The command does not mutate ticket state.

## Dependencies

- `PLF-019` — shared advisory artifact conventions.
- `PLF-020` — read-only child Pi runner.
- `PLF-029` — parent LLM handoff convention for advisory command results.

## Implementation Notes

Likely files:

- `.pi/extensions/ticket-workflow/`
- `.pi/extensions/ticket-workflow/prompts/readiness.md` or equivalent inline prompt helper
- `tickets/.artifacts/readiness/`
- `docs/ticket-workflow-commands.md`

The prompt should ask the child to be useful but not authoritative. It should not tell the child to move files.

## Verification

- `npm --prefix .pi/extensions exec tsc -- -p .pi/extensions/tsconfig.json --noEmit` passed.
- `pi --approve --offline --no-session -p '/ticket-status'` passed and reported `PLF-021` as the active ticket during implementation.
- `pi --approve --offline --no-session -p '/ticket-readiness PLF-021'` refused clearly because `PLF-021` is in `tickets/ongoing/`, not `tickets/backlog/`.
- `pi --approve --no-session -p '/ticket-readiness PLF-022'` generated `tickets/.artifacts/readiness/PLF-022.md`, displayed a readiness brief, and printed the suggested parent handoff in print mode.
- Manual artifact inspection confirmed the generated readiness brief included advisory frontmatter, source ticket path, state, and SHA-256 fingerprint.
- The generated `PLF-022` readiness artifact was removed after inspection to keep this commit focused; no `PLF-022` ticket file moved or changed.
- Final `pi --approve --offline --no-session -p '/ticket-status'` passed after moving the ticket to completed and reported no active ticket.
- Final `npm --prefix .pi/extensions exec tsc -- -p .pi/extensions/tsconfig.json --noEmit` and `git diff --check` passed.

## Files Changed

- `.pi/extensions/ticket-workflow/index.ts`
- `docs/ticket-workflow-commands.md`
- `tickets/completed/PLF-021.md`

## Decisions

- Implemented `/ticket-readiness <ticket-id>` as a read-only advisory command that only accepts tickets in `tickets/backlog/`.
- Kept child analysis read-only through the existing `runReadOnlyChildPiAdvisory(...)` safety boundary; the parent command writes the advisory artifact.
- Used existing advisory artifact metadata helpers so readiness briefs include the ticket path, state, and SHA-256 fingerprint.
- Used `deliverAdvisoryParentHandoff(...)` so interactive modes hand results to the parent LLM and print mode emits a suggested handoff.

## Notes

This is the first high-value advisory command and should remain lightweight in daily use.

## Resolution

Implemented `/ticket-readiness <ticket-id>` with backlog-only validation, read-only child Pi analysis, readiness artifact generation, and parent LLM handoff behavior. The final commit message includes `PLF-021`; the commit identifier is discoverable with `git log --grep='PLF-021'`.

## Log

- 2026-06-24 14:30:12: Ticket created in backlog from the sub-Pi ticketization proposal for the ticket workflow command system.
- 2026-06-24 16:01:48: Updated to depend on `PLF-029` so readiness briefs are handed back to the parent LLM instead of only being displayed or written as artifacts.
- 2026-06-24 16:20:48: Refined after human validation. Dependencies `PLF-019`, `PLF-020`, and `PLF-029` are complete; the minimal implementation should add only the backlog readiness advisory command and its documentation.
- 2026-06-24 16:20:48: Moved to planned after readiness review and human approval of the minimal `PLF-021` plan.
- 2026-06-24 16:20:48: Activated for implementation after confirming no other ongoing or planned tickets exist.
- 2026-06-24 16:25:52: Implemented `/ticket-readiness`, updated command documentation, and verified TypeScript, ticket status, backlog-only refusal, artifact generation, and print-mode parent handoff.
- 2026-06-24 16:26:31: Moved to completed after implementation and verification; final commit pending.
- 2026-06-24 16:26:45: Final `/ticket-status`, TypeScript, and `git diff --check` verification passed with no active ticket.
