---
id: PLF-029
title: "Define parent LLM handoff for advisory ticket commands"
type: infrastructure
created: 2026-06-24 16:01:48
dependencies: ["PLF-019", "PLF-020", "PLF-028"]
---

# PLF-029: Define parent LLM handoff for advisory ticket commands

## Objective

Define and implement the minimal parent LLM handoff convention for future advisory ticket workflow commands.

## Context

`PLF-020` added a read-only child Pi advisory runner, and `PLF-028` made `/ticket-child-diagnostic` visibly acknowledge child process startup. Manual UX review showed another important gap: command output displayed as custom command messages is useful to humans, but it is not automatically reworked by the parent LLM as a normal tool result would be.

The advisory command family should not only display child output. When a command produces a verdict, plan, verification result, or recommendation, it should hand the advisory result back to the parent LLM so the main conversation can synthesize it, explain what matters, and ask for human validation before any state transition.

## Scope

In scope:

- Document the parent LLM handoff rule for advisory ticket commands.
- Define when advisory commands should use `sendUserMessage(...)` to trigger a parent LLM follow-up.
- Define what the handoff message must contain: original request, artifact path when present, advisory summary or child output, advisory-only notice, and explicit instruction not to mutate ticket state without human approval.
- Define print/non-interactive behavior: do not trigger a parent LLM follow-up; instead print a suggested handoff message.
- Add a small shared helper in the ticket workflow extension for building or sending advisory parent handoff messages if it keeps later commands consistent.
- Update backlog tickets for future advisory commands so they depend on this convention where appropriate.

Out of scope:

- Implementing `/ticket-readiness`, `/ticket-plan`, `/ticket-verify`, `/ticket-activate-check`, `/ticket-completion-brief`, or `/ticket-doctor`.
- Changing the read-only child Pi runner safety boundary.
- Adding child output streaming.
- Automatically moving tickets, committing changes, or approving workflow transitions.
- Making `/ticket-status` trigger a parent LLM follow-up.

## Acceptance Criteria

- [x] `docs/ticket-workflow-commands.md` documents the advisory parent handoff pattern.
- [x] The documentation distinguishes deterministic display-only commands from advisory commands that should trigger a parent LLM follow-up.
- [x] The convention says advisory commands should use parent LLM handoff for verdicts, plans, verification results, and transition recommendations.
- [x] The convention says print/non-interactive mode must not trigger a hidden LLM follow-up and should print a suggested handoff instead.
- [x] A shared helper or clearly documented message template exists for future advisory commands.
- [x] Tickets `PLF-021` through `PLF-025` explicitly account for the parent handoff convention.
- [x] `PLF-026` remains documented as deterministic/read-only without parent handoff by default.
- [x] TypeScript extension checking passes if extension code changes.

## Dependencies

- `PLF-019` — advisory artifact conventions define durable outputs referenced by parent handoffs.
- `PLF-020` — read-only child Pi advisory runner supplies advisory content.
- `PLF-028` — immediate diagnostic feedback establishes the visible command-start UX pattern.

## Implementation Notes

Likely files:

- `.pi/extensions/ticket-workflow/index.ts` if a shared helper is added.
- `docs/ticket-workflow-commands.md`.
- `tickets/backlog/PLF-021.md` through `tickets/backlog/PLF-026.md` if additional dependency or scope clarifications are needed.
- `tickets/completed/PLF-029.md` when completed.

A minimal helper can build a Markdown handoff message and optionally call `pi.sendUserMessage(...)` only when the command is not running in print mode. Keep the helper small; later command tickets should decide exactly when to call it.

Suggested handoff content:

```markdown
A ticket workflow advisory command completed.

Original request:
<request>

Advisory artifact:
<path or none>

Advisory result:
<summary or child output>

This is advisory only. Ticket files and ticket directories remain authoritative.
Please synthesize the result for the user, explain what matters, propose the next action, and do not mutate ticket state without explicit human approval.
```

## Verification

- Reviewed `docs/ticket-workflow-commands.md`; it now documents command interaction classes, the advisory parent handoff template, required contents, and print/non-interactive behavior.
- `npm --prefix .pi/extensions exec tsc -- -p .pi/extensions/tsconfig.json --noEmit` passed after adding the TypeScript helper.
- `pi --approve --offline --no-session -p '/ticket-status'` passed and reported `PLF-029` as the active ticket during implementation.
- Final `pi --approve --offline --no-session -p '/ticket-status'` passed after moving the ticket to completed and reported no active ticket.
- `git diff --check` passed.
- `rg "registerCommand" .pi/extensions/ticket-workflow/index.ts` confirmed no ticket workflow state transition command was added.
- Reviewed `PLF-021` through `PLF-025`; they already depend on `PLF-029` and require parent handoff in their scope and acceptance criteria.
- Reviewed `PLF-026`; it remains documented as deterministic/read-only without parent handoff by default.

## Files Changed

- `.pi/extensions/ticket-workflow/index.ts`
- `docs/ticket-workflow-commands.md`
- `tickets/completed/PLF-029.md`

## Decisions

- Added shared `buildAdvisoryParentHandoffMessage(...)` and `deliverAdvisoryParentHandoff(...)` helpers so future advisory commands can use one message shape.
- Treat `tui` and `rpc` as interactive modes where `pi.sendUserMessage(...)` may be used.
- Treat `print` and `json` as non-interactive modes; print mode writes a suggested handoff, and other non-interactive modes should include the suggested handoff in the explicit command result.
- Queue handoff with `{ deliverAs: "followUp" }` when the parent agent is busy.
- Left `/ticket-status`, `/ticket-child-diagnostic`, and the child Pi runner safety boundary unchanged.

## Notes

This ticket should be implemented before `PLF-021` so the first real advisory command does not repeat the diagnostic command's display-only UX limitation.

## Resolution

Implemented the advisory parent handoff convention in documentation and added shared helper functions for future advisory commands. The final commit message includes `PLF-029`; the commit identifier is discoverable with `git log --grep='PLF-029'`.

## Log

- 2026-06-24 16:01:48: Ticket created in planned from manual UX review and read-only child Pi analysis of how future advisory ticket commands should feed results back to the parent LLM.
- 2026-06-24 16:07:11: Moved to ongoing after confirming no active ticket, dependencies are completed, and the user approved the minimal implementation plan.
- 2026-06-24 16:09:29: Implemented advisory parent handoff documentation and shared helper functions; TypeScript, ticket status, diff, and command-scope checks passed.
- 2026-06-24 16:10:07: Moved to completed after implementation and verification; final commit pending.
