---
id: PLF-019
title: "Define advisory artifact conventions for ticket commands"
type: infrastructure
created: 2026-06-24 14:30:12
dependencies: ["PLF-018"]
---

# PLF-019: Define advisory artifact conventions for ticket commands

## Objective

Define the minimal storage layout, metadata, and helper behavior for advisory artifacts produced by future ticket workflow commands.

## Context

The planned command system should use child Pi sessions mostly as read-only advisors. Their outputs must be durable and easy to inspect, but they must not replace ticket files or ticket directories as the source of truth.

A shared convention is needed before adding readiness, implementation-plan, or verification commands so each command does not invent its own artifact format.

## Scope

In scope:

- Define an advisory artifact location, likely under `tickets/.artifacts/`.
- Add subdirectories for readiness briefs, implementation plans, verification briefs, and possibly completion briefs.
- Document that artifacts are advisory only.
- Add helper logic for deterministic artifact paths.
- Add helper logic to compute or record enough ticket fingerprint information to detect stale artifacts.
- Include generated timestamp, command name, ticket ID, ticket path, ticket state, and advisory notice in artifact metadata where practical.

Out of scope:

- Generating real readiness, implementation-plan, or verification content.
- Child Pi process orchestration.
- Ticket state transitions.
- Commit automation.
- Treating artifacts as authoritative workflow state.

## Acceptance Criteria

- [x] The chosen artifact layout is documented.
- [x] Artifact paths are deterministic for a ticket ID and artifact type.
- [x] Artifact metadata includes enough information to identify the source ticket and detect likely staleness.
- [x] Documentation clearly states that ticket files and their directories remain authoritative.
- [x] The implementation does not repurpose ticket state directories for artifacts.

## Dependencies

- `PLF-018` — the ticket workflow extension foundation should exist first.

## Implementation Notes

Likely files:

- `.pi/extensions/ticket-workflow/`
- `tickets/.artifacts/README.md`
- `tickets/.artifacts/readiness/.gitkeep`
- `tickets/.artifacts/plans/.gitkeep`
- `tickets/.artifacts/verification/.gitkeep`
- `tickets/.artifacts/completion/.gitkeep` if included
- `docs/ticket-workflow-commands.md`

Keep metadata small and inspectable. A content hash of the source ticket is preferable to relying only on modification time.

## Verification

- Manual review confirmed `tickets/.artifacts/README.md` documents the artifact layout, metadata, staleness rules, and advisory-only status.
- Manual review confirmed `docs/ticket-workflow-commands.md` documents the command conventions and source-of-truth rule.
- `pi --approve --offline --no-session -p '/ticket-status'` passed after the extension changes and reported `PLF-019` as the only active ticket.
- `pi --approve --offline --no-session -p '/ticket-status'` passed after moving the ticket to completed and reported no active ticket.
- Helper output is not exposed through a command yet; the extension loads successfully with the helper exports present.

## Files Changed

- `.pi/extensions/ticket-workflow/index.ts`
- `docs/ticket-workflow-commands.md`
- `tickets/.artifacts/README.md`
- `tickets/.artifacts/readiness/.gitkeep`
- `tickets/.artifacts/plans/.gitkeep`
- `tickets/.artifacts/verification/.gitkeep`
- `tickets/.artifacts/completion/.gitkeep`
- `tickets/completed/PLF-019.md`

## Decisions

- Chose `tickets/.artifacts/` so generated advisory files do not live in ticket state directories.
- Chose one Markdown artifact per ticket and artifact type using `tickets/.artifacts/<artifact-type>/<ticket-id>.md`.
- Included `readiness`, `plans`, `verification`, and `completion` as the initial artifact types.
- Used a SHA-256 hash of the source ticket file as the primary staleness fingerprint.
- Added reusable helpers without adding a new user-facing command in this ticket.

## Notes

`PLF-018` is complete, so this ticket can establish shared conventions before later advisory commands produce durable files.

## Resolution

Implemented advisory artifact conventions, documentation, directory scaffolding, and reusable extension helpers for deterministic artifact paths and ticket fingerprints.

## Log

- 2026-06-24 14:30:12: Ticket created in backlog from the sub-Pi ticketization proposal for the ticket workflow command system.
- 2026-06-24 15:00:17: Refined after human validation. Dependency `PLF-018` is complete; the minimal implementation should define advisory artifact layout, documentation, and reusable path/fingerprint helpers only.
- 2026-06-24 15:00:30: Moved to planned after readiness review and human approval of the minimal PLF-019 plan.
- 2026-06-24 15:00:30: Activated for implementation after confirming no other ongoing or planned tickets exist.
- 2026-06-24 15:02:45: Implemented advisory artifact documentation, directory scaffolding, and reusable extension helpers; verified `/ticket-status` still works.
- 2026-06-24 15:03:07: Moved to completed after implementation and verification; final commit pending.
- 2026-06-24 15:03:13: Final `/ticket-status` check passed with no active ticket.
